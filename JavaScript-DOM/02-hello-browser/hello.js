export function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}


/*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*/

/*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*//*
function hello(name) {
  return `Hello ${name.toUpperCase()}`;
}
*/
